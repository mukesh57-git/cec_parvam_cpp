/*
 * =====================================================================
 * 💻 STUDENT TASK: THE GARAGE
 * =====================================================================
 * 
 * YOUR MISSION:
 * 1. Look at the `Vehicle` Base class. It has a `drive()` function.
 * 2. Create a `Car` class that INHERITS from `Vehicle` using `public Vehicle`.
 * 3. Give the `Car` class its own special function called `honkHorn()`.
 * 4. In `main()`, spawn a `Car` object.
 * 5. Make the Car honk its horn (its own function) AND make it drive 
 *    (the inherited function).
 */

#include <iostream>
using namespace std;

// 1. THE BASE CLASS
class Vehicle {
public:
    void drive() {
        cout << "The vehicle is moving forward." << endl;
    }
};

// 🛑 2. CREATE THE DERIVED 'Car' CLASS HERE!
// (Make sure it inherits from Vehicle)






int main() {
    cout << "--- THE GARAGE ---" << endl;

    // 🛑 3. Spawn a Car object here!
    
    
    // 🛑 4. Call the Car's own honkHorn() function!
    
    
    // 🛑 5. Call the inherited drive() function!
    

    return 0;
}
