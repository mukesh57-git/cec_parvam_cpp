/*
 * =====================================================================
 * 💻 STUDENT TASK: THE ANIMAL KINGDOM
 * =====================================================================
 * 
 * YOUR MISSION:
 * 1. Create an Abstract Class called `Animal`.
 * 2. Give it a Pure Virtual Function called `makeSound() = 0;`.
 * 3. Create a `Lion` class that inherits from `Animal`. Make it roar!
 * 4. Create a `Cow` class that inherits from `Animal`. Make it moo!
 * 5. In main(), create a Lion and a Cow and ask them to make a sound.
 */

#include <iostream>
using namespace std;

// 🛑 1. Create the Abstract Class 'Animal' here:
// Remember to include: virtual void makeSound() = 0;




// 🛑 2. Create the 'Lion' class that inherits from Animal here:




// 🛑 3. Create the 'Cow' class that inherits from Animal here:




int main() {
    cout << "--- ANIMAL KINGDOM ---" << endl;

    // 🛑 4. Create a Lion object and call makeSound()
    
    
    // 🛑 5. Create a Cow object and call makeSound()
    

    // 🛑 What happens if you try to do: Animal a; ?
    // Try it! The compiler should block you because Animal is Abstract!

    return 0;
}
