/*
 * =====================================================================
 * 💻 STUDENT TASK: THE ENEMY SPAWNER
 * =====================================================================
 * 
 * YOUR MISSION:
 * 1. Ask the user: "How many zombies are attacking?"
 * 2. Read their answer into an integer variable called `zombieCount`.
 * 3. Dynamically allocate an array of integers on the Heap using `zombieCount` as the size.
 *    Name the array pointer `zombieHealths`.
 * 4. Use a for-loop to set every zombie's health to 100.
 * 5. Use another for-loop to print out all the healths.
 * 6. VERY IMPORTANT: Clean up the memory to prevent a memory leak!
 */

#include <iostream>
using namespace std;

int main() {
    cout << "--- ZOMBIE SPAWNER ---" << endl;

    // 🛑 1 & 2. Ask the user for the number of zombies and store it
    
    
    
    // 🛑 3. Dynamically allocate the array on the Heap using 'new'
    
    
    
    // 🛑 4. Fill the array with 100s
    
    
    
    // 🛑 5. Print the array
    
    
    
    // 🛑 6. Clean up the memory! (Don't forget the brackets!)
    
    
    

    return 0;
}
