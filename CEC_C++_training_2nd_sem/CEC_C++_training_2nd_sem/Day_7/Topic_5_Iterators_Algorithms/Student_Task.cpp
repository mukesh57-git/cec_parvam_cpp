/*
 * =====================================================================
 * 💻 STUDENT TASK: THE DATA ANALYST
 * =====================================================================
 * 
 * YOUR MISSION:
 * You have been given a vector containing the test scores for a class.
 * 
 * 1. Use the `sort()` algorithm to sort the scores.
 * 2. Use the `reverse()` algorithm so the highest scores are at the front!
 * 3. Use a Range-Based For Loop to print all the scores on one line.
 * 4. Use the `count()` algorithm to figure out how many students scored a perfect 100.
 *    Print that number out!
 */

#include <iostream>
#include <vector>
#include <algorithm> // Don't forget the spellbook!

using namespace std;

int main() {
    cout << "--- CLASS TEST SCORES ---" << endl;
    
    vector<int> scores = {85, 100, 42, 99, 100, 75, 88, 100, 60};

    // 🛑 1. Sort the scores using sort()
    
    
    // 🛑 2. Reverse the scores using reverse()
    
    
    // 🛑 3. Print the scores using a Range-Based For Loop (for int x : scores)
    
    
    
    // 🛑 4. Count how many 100s there are using count(), and print the result!
    
    
    

    return 0;
}
