/*
 * =====================================================================
 * 💻 STUDENT TASK: THE OPERATING SYSTEM
 * =====================================================================
 * 
 * YOUR MISSION:
 * 1. Create a `vector<int>` to store the player's high scores.
 *    - Add 500, 750, and 1200 to the vector.
 *    - Print out the total number of scores using `.size()`.
 * 
 * 2. Create a `stack<string>` to act as an "Undo" feature for a text editor.
 *    - Push the actions: "Typed 'A'", "Typed 'B'", "Deleted 'A'".
 *    - The user clicks UNDO. Remove the last action, and print out what is 
 *      currently on the top of the stack.
 * 
 * 3. Create a `queue<string>` to manage a Printer queue.
 *    - Push three documents: "Essay.pdf", "Photo.jpg", "Resume.pdf".
 *    - The printer finishes the first job. Remove it from the queue, 
 *      and print the name of the NEXT document waiting in line!
 */

#include <iostream>
#include <vector>
#include <stack>
#include <queue>
#include <string>

using namespace std;

int main() {
    cout << "--- 1. HIGH SCORES (Vector) ---" << endl;
    // 🛑 Write your vector code here:
    
    
    
    cout << "\n--- 2. TEXT EDITOR (Stack) ---" << endl;
    // 🛑 Write your stack code here:
    
    
    
    cout << "\n--- 3. PRINTER QUEUE (Queue) ---" << endl;
    // 🛑 Write your queue code here:
    
    
    
    
    return 0;
}
