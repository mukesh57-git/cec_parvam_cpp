/*
 * =====================================================================
 * 💻 STUDENT TASK: THE SMART HOME
 * =====================================================================
 * 
 * YOUR MISSION:
 * 1. Create an Abstract Base Class called `Appliance`.
 *    - Give it a Pure Virtual Function: `virtual void turnOn() = 0;`
 * 
 * 2. Create a class `WashingMachine` that inherits from `Appliance`.
 *    - Override `turnOn()` to print: "Washing Machine is filling with water..."
 * 
 * 3. Create a class `Microwave` that inherits from `Appliance`.
 *    - Override `turnOn()` to print: "Microwave is heating food... MMMM..."
 * 
 * 4. In main(), create an array of `Appliance*` pointers, store one of each 
 *    appliance inside it, and use a loop to turn them all on!
 */

#include <iostream>
#include <vector>
using namespace std;

// 🛑 1. Create the Abstract Base Class 'Appliance' here!





// 🛑 2. Create the 'WashingMachine' class here!





// 🛑 3. Create the 'Microwave' class here!





int main() {
    cout << "--- SMART HOME SYSTEM ---" << endl;

    // 🛑 4. Create a vector of Appliance pointers, add the appliances, and loop through them!
    
    
    
    

    return 0;
}
